description = 'JobsUI UI'

dependencies {
    compile project(':jobsui-core')
    compile project(':jobsui-edit')

    testCompile "org.testfx:testfx-core:4.0.+"
    testCompile "org.testfx:testfx-junit:4.0.+", {
        exclude group: "junit", module: "junit"
    }

}
